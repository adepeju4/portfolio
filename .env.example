PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Read token for local draft preview (npm run dev). Leave blank to disable.
# Create one at https://sanity.io/manage -> your project -> API -> Tokens (Viewer role).
SANITY_API_READ_TOKEN=
